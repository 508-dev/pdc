"""The explorer: a web interface over the kernel.

A shell, like `cli`. Nothing in the kernel imports it, and a layering test
keeps it that way.

It exists because the audit right in D-010 is only real if checking the work
does not require a terminal. A tool usable solely by the CLI-literate
reproduces expert authority — the same shape as the audit-firm priesthood
D-007 exists to avoid, with programmers in the role.
"""
